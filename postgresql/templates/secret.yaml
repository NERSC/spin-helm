apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgresql.fullname" . }}-env
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ .Values.env.POSTGRES_PASSWORD | quote }}
